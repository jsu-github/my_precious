---
phase: 02-data-model-api
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types.ts
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05, DATA-06, DATA-07, DATA-08, DATA-09, DATA-10]

must_haves:
  truths:
    - "All entity interfaces are typed and exported from types.ts"
    - "Asset class and tax status are typed enums"
    - "Input/create types use Pick<> or Omit<> — not duplicate interfaces"
  artifacts:
    - path: "frontend/src/types.ts"
      provides: "Entity, Asset, AssetLocation, Acquisition, FiscalTag, Transfer, ValuationSnapshot interfaces + enums"
      exports: ["Entity", "Asset", "AssetLocation", "Acquisition", "FiscalTag", "Transfer", "ValuationSnapshot", "AssetClass", "TaxStatus"]
  key_links:
    - from: "frontend/src/api.ts"
      to: "frontend/src/types.ts"
      via: "import { Entity, Asset, ... }"
      pattern: "import.*types"
---

<objective>
Define all TypeScript interfaces that model the database schema for the six entities. These types are consumed by api.ts helpers and all future page components.

Purpose: Types are the contract between the API and frontend. Writing them first (Wave 1, parallel to migrations) means api.ts and components can be built against a stable interface.
Output: Complete types.ts with all entity interfaces, enums, and input types.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-data-model-api/02-CONTEXT.md
@.planning/phases/01-foundation/01-04-SUMMARY.md
@frontend/src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define all entity interfaces and enums</name>
  <files>frontend/src/types.ts</files>
  <action>
Replace the stub `types.ts` with the full type definitions. Keep `ApiError`.

```typescript
// ─── Error shape (from API errorHandler) ────────────────────────────────────
export interface ApiError {
  error: { message: string; status: number };
}

// ─── Enums (stored as string in DB) ──────────────────────────────────────────
export type AssetClass =
  | 'precious_metals'
  | 'real_estate'
  | 'equities'
  | 'crypto'
  | 'private_equity'
  | 'fixed_income'
  | 'cash'
  | 'exotics';

export type EntityType = 'personal' | 'business';
export type TaxStatus = 'settled' | 'pending';
export type SecurityClass = 'high_security' | 'medium_security' | 'standard';
export type AuditFrequency = 'annual' | 'semi_annual' | 'quarterly';

// ─── Core entities ────────────────────────────────────────────────────────────
export interface Entity {
  id: number;
  type: EntityType;
  name: string;
  description: string | null;
  created_at: string;
  updated_at: string;
}

export interface AssetLocation {
  id: number;
  name: string;
  country_code: string;
  custodian_name: string;
  map_x_pct: number;
  map_y_pct: number;
  created_at: string;
  updated_at: string;
}

export interface Asset {
  id: number;
  entity_id: number;
  location_id: number | null;
  name: string;
  asset_class: AssetClass;
  current_value: string; // NUMERIC returned as string by pg driver
  security_class: SecurityClass;
  audit_frequency: AuditFrequency;
  last_audit_date: string | null;
  description: string | null;
  created_at: string;
  updated_at: string;
  // Joined fields (present when fetched with details)
  entity?: Entity;
  location?: AssetLocation;
  acquisitions?: Acquisition[];
}

export interface Acquisition {
  id: number;
  asset_id: number;
  purchase_date: string;
  cost_basis: string; // NUMERIC as string
  quantity: string;   // NUMERIC as string
  tax_status: TaxStatus;
  description: string | null;
  created_at: string;
  updated_at: string;
}

export interface FiscalTag {
  id: number;
  asset_id: number;
  fiscal_year: number;
  fiscal_category: string;
  jurisdiction: string;
  notes: string | null;
  created_at: string;
  updated_at: string;
}

export interface Transfer {
  id: number;
  from_entity_id: number;
  to_entity_id: number;
  amount: string; // NUMERIC as string
  transfer_date: string;
  description: string | null;
  created_at: string;
  updated_at: string;
  from_entity?: Entity;
  to_entity?: Entity;
}

export interface ValuationSnapshot {
  id: number;
  asset_id: number;
  value: string; // NUMERIC as string
  snapshotted_at: string;
  created_at: string;
}

// ─── Input types (for POST/PUT) ───────────────────────────────────────────────
export type CreateEntity = Omit<Entity, 'id' | 'created_at' | 'updated_at'>;
export type UpdateEntity = Partial<CreateEntity>;

export type CreateAssetLocation = Omit<AssetLocation, 'id' | 'created_at' | 'updated_at'>;
export type UpdateAssetLocation = Partial<CreateAssetLocation>;

export type CreateAsset = Omit<Asset, 'id' | 'created_at' | 'updated_at' | 'entity' | 'location' | 'acquisitions'>;
export type UpdateAsset = Partial<CreateAsset>;

export type CreateAcquisition = Omit<Acquisition, 'id' | 'asset_id' | 'created_at' | 'updated_at'>;
export type UpdateAcquisition = Partial<CreateAcquisition>;

export type CreateFiscalTag = Omit<FiscalTag, 'id' | 'asset_id' | 'created_at' | 'updated_at'>;
export type UpdateFiscalTag = Partial<CreateFiscalTag>;

export type CreateTransfer = Omit<Transfer, 'id' | 'created_at' | 'updated_at' | 'from_entity' | 'to_entity'>;

export type CreateValuationSnapshot = Pick<ValuationSnapshot, 'value'>;

// ─── Dashboard summary ────────────────────────────────────────────────────────
export interface DashboardSummary {
  total_value: string;
  entity_id?: number;
  by_asset_class: Array<{ asset_class: AssetClass; total_value: string; count: number }>;
  asset_count: number;
}
```

NOTE: The pg driver returns NUMERIC columns as JavaScript strings to preserve precision. Components must use `parseFloat()` or `Number()` when doing arithmetic.
  </action>
  <verify>
    <automated>grep -c "export interface\|export type" /home/jsu/projects/precious_dashboard/frontend/src/types.ts</automated>
  </verify>
  <done>types.ts exports Entity, Asset, AssetLocation, Acquisition, FiscalTag, Transfer, ValuationSnapshot + enums + input types + DashboardSummary</done>
</task>

</tasks>

<verification>
`grep -c "export" frontend/src/types.ts` returns ≥ 20 exports.
</verification>

<success_criteria>
All entity types exported. No TypeScript errors in types.ts. Input types use Omit/Partial — no duplicated fields.
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-model-api/02-02-SUMMARY.md`
</output>
