---
phase: 04-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/DashboardPage.tsx
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05]

must_haves:
  truths:
    - "Net worth hero shows formatted total portfolio value"
    - "Entity toggle (Personal/Business/Global) recalculates the displayed total"
    - "Asset allocation panel shows per-class breakdowns with color bars"
    - "Vault Security panel shows custodian name + Connected status + last-audit per location"
  artifacts:
    - path: "frontend/src/pages/DashboardPage.tsx"
      provides: "Full dashboard page with 3 panels"
  key_links:
    - from: "DashboardPage.tsx"
      to: "api.assets.list + api.locations.list"
      via: "useEffect Promise.all fetch on mount + entityFilter change"
---

<objective>
Build the Dashboard page: net worth hero card (glassmorphism), asset allocation breakdown per class, and vault security status per location.

Purpose: First live data screen — proves the pipeline from DB → API → frontend works end-to-end.
Output: frontend/src/pages/DashboardPage.tsx (full implementation)
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-app-shell/03-02-SUMMARY.md
@frontend/src/types.ts
@frontend/src/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: DashboardPage — hero card + data fetching</name>
  <files>frontend/src/pages/DashboardPage.tsx</files>
  <action>
    Full replacement of the Phase 3 stub. Fetch assets + locations via Promise.all on mount.
    Compute filtered values with useMemo based on entityFilter prop.
    Render glassmorphism hero card with total value in font-headline italic, asset count chip, last-updated label.
    Use EntityFilter type from layouts/AppShell.
  </action>
  <verify>curl http://localhost:4000 returns 200; no TS errors in docker logs</verify>
  <done>Dashboard renders hero card with live total from API</done>
</task>

<task type="auto">
  <name>Task 2: Asset allocation panel + vault security panel</name>
  <files>frontend/src/pages/DashboardPage.tsx</files>
  <action>
    Asset allocation: grouped by asset_class, sorted by value descending.
    Each class: color dot + label + horizontal bar (width = percentage) + value + pct.
    Vault security: locations list, each showing custodian name, "● Connected" (emerald), last_audit_date.
    Layout: 2-col grid (lg:grid-cols-3) — allocation takes 2 cols, vault takes 1.
  </action>
  <verify>Asset allocation shows bars, vault shows location list; entity toggle changes displayed values</verify>
  <done>All 3 panels visible; entity toggle updates totals</done>
</task>

</tasks>

<success_criteria>
1. formatCurrency($15,450,000) renders correctly in hero
2. Personal/Business toggle recomputes from filtered asset list
3. Asset allocation bars render with correct proportions
4. Vault security shows 3 locations (from seed) with Connected status
</success_criteria>
