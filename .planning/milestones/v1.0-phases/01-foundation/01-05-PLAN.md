---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - 01-03
  - 01-04
files_modified: []
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "make dev starts all 3 containers without errors"
    - "GET localhost:3001/health returns { status: 'ok' } HTTP 200"
    - "API logs show '[db] Migrations complete' on startup"
    - "make clean removes volumes; make dev after clean starts a fresh database"
    - "Browser at localhost:3000 shows the React placeholder page"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Verified working container orchestration"
    - path: "Makefile"
      provides: "Verified dev commands"
  key_links:
    - from: "make dev"
      to: "localhost:3001/health"
      via: "docker compose up → api container → Express /health route"
      pattern: "status.*ok"
---

<objective>
Verify the full Phase 1 Foundation is working end-to-end.

Purpose: Human confirms `make dev` starts all containers, the API health endpoint responds, the database auto-migrates, and the frontend serves in the browser.
Output: No files modified — verification only.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 1 Foundation stack:
- docker-compose.yml: db (PostgreSQL :5432), api (Express :3001), frontend (Vite :3000)
- Makefile: dev, up, down, clean, logs-api, migrate
- API: runMigrations on startup, /health endpoint, { error: { message, status } } error handler
- Frontend: Vite React TypeScript scaffold, /api proxy, types.ts + api.ts stubs
- 001_initial.ts migration (empty — confirms migration pipeline)
  </what-built>
  <how-to-verify>
Run these steps in order:

1. **Start the stack:**
   ```bash
   make dev
   ```
   Expected: Docker builds API + frontend images, starts all 3 containers. Watch for:
   - `[db] Migrations complete` in API logs
   - `[api] Listening on :3001` in API logs
   - `VITE ➜  Local: http://localhost:3000/` in frontend logs

2. **Verify API health:**
   ```bash
   curl http://localhost:3001/health
   ```
   Expected: `{"status":"ok"}`

3. **Verify frontend:**
   Open http://localhost:3000 in browser.
   Expected: Page shows "Precious Dashboard" and "Phase 1: Infrastructure running."

4. **Verify clean reset:**
   ```bash
   make clean
   make dev
   ```
   Expected: Second `make dev` starts fresh — no migration errors, DB starts empty.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All 6 Phase 1 success criteria must be TRUE:
1. `make dev` starts all three containers (frontend :3000, API :3001, PostgreSQL :5432) without errors
2. `make clean` stops all containers and removes volumes; subsequent `make dev` starts fresh
3. `GET http://localhost:3001/health` returns 200 JSON with `{ "status": "ok" }`
4. API console logs confirm migrations complete on startup ("Migrations complete")
5. NUMERIC(20, 2) convention is documented in db.ts and 001_initial.ts
6. API error responses use `{ error: { message: string, status: number } }` shape
</verification>

<success_criteria>
Human approves all verification checks.
</success_criteria>

<output>
After human approval, create `.planning/phases/01-foundation/01-05-SUMMARY.md` confirming Phase 1 complete, then update `.planning/STATE.md` to mark Phase 1 done and set Current Phase to 2.
</output>
