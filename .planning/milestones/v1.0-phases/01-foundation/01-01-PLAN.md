---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/package.json
  - api/tsconfig.json
  - api/Dockerfile
  - api/src/db.ts
autonomous: true
requirements:
  - INFRA-05

must_haves:
  truths:
    - "api/src/db.ts exports a Knex singleton connected to PostgreSQL via DATABASE_URL"
    - "api/src/db.ts exports runMigrations() that calls knex.migrate.latest()"
    - "api/package.json includes express, knex, pg, cors as dependencies"
    - "api/Dockerfile runs ts-node-dev for dev hot-reload"
  artifacts:
    - path: "api/package.json"
      provides: "Node.js dependency manifest for API"
    - path: "api/tsconfig.json"
      provides: "TypeScript compiler config for API"
    - path: "api/Dockerfile"
      provides: "Container build instructions for API service"
    - path: "api/src/db.ts"
      provides: "Knex singleton and runMigrations export"
      exports: ["knex", "runMigrations"]
  key_links:
    - from: "api/src/db.ts"
      to: "PostgreSQL"
      via: "Knex client 'pg' using DATABASE_URL"
      pattern: "process.env.DATABASE_URL"
    - from: "api/src/db.ts"
      to: "api/migrations/"
      via: "knex.migrate.latest()"
      pattern: "migrate.latest"
---

<objective>
Set up the API package configuration and Knex database layer.

Purpose: Establish the Node.js package manifest, TypeScript config, Dockerfile, and the Knex singleton with auto-migration export. This is the foundation all API source code depends on.
Output: api/package.json, api/tsconfig.json, api/Dockerfile, api/src/db.ts
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/codebase/ARCHITECTURE.md
@.github/copilot-instructions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create api/package.json + api/tsconfig.json</name>
  <files>api/package.json, api/tsconfig.json</files>
  <action>
Create `api/` directory if it doesn't exist. Create `api/package.json`:

```json
{
  "name": "precious-api",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "migrate": "knex migrate:latest"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "knex": "^3.1.0",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}
```

Create `api/tsconfig.json`:

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "lib": ["ES2020"],
    "outDir": "./dist",
    "rootDir": "./src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true
  },
  "include": ["src/**/*", "migrations/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

Note: migrations/ is included in tsconfig so ts-node-dev can run migration files directly.
  </action>
  <verify>
    <automated>cat api/package.json | grep '"name"' && cat api/tsconfig.json | grep '"strict"'</automated>
  </verify>
  <done>api/package.json and api/tsconfig.json exist with correct content</done>
</task>

<task type="auto">
  <name>Task 2: Create api/Dockerfile</name>
  <files>api/Dockerfile</files>
  <action>
Create `api/Dockerfile` for development (hot-reload with ts-node-dev). This Dockerfile is used by docker-compose in dev mode — source is volume-mounted so node_modules stay in container:

```dockerfile
FROM node:20-alpine
WORKDIR /app

# Install dependencies into the container image layer
# (source files are volume-mounted from host at runtime)
COPY package*.json ./
RUN npm ci

# Expose API port
EXPOSE 3001

# Hot-reload dev server — source mounted via docker-compose volume
CMD ["npm", "run", "dev"]
```
  </action>
  <verify>
    <automated>cat api/Dockerfile | grep "CMD"</automated>
  </verify>
  <done>api/Dockerfile exists and specifies npm run dev as CMD</done>
</task>

<task type="auto">
  <name>Task 3: Create api/src/db.ts</name>
  <files>api/src/db.ts</files>
  <action>
Create `api/src/` directory. Create `api/src/db.ts` — the Knex singleton and migration runner.

CRITICAL: db.ts establishes the NUMERIC(20, 2) convention for all monetary columns. Every migration that creates monetary fields MUST use `table.decimal('column_name', 20, 2)` — never `table.float()` or `table.double()`. This is convention INFRA-05.

```typescript
import Knex from 'knex';
import path from 'path';

export const knex = Knex({
  client: 'pg',
  connection: process.env.DATABASE_URL || 'postgres://precious:precious@localhost:5432/precious',
  migrations: {
    directory: path.join(__dirname, '../migrations'),
    // ts-node-dev runs .ts migrations directly; build output uses .js
    extension: process.env.NODE_ENV === 'production' ? 'js' : 'ts',
  },
});

/**
 * Run all pending Knex migrations.
 * Called automatically at API startup in src/index.ts.
 *
 * MONETARY COLUMN CONVENTION (INFRA-05):
 *   All money columns MUST use: table.decimal('column_name', 20, 2)
 *   NEVER use: table.float() or table.double()
 */
export async function runMigrations(): Promise<void> {
  await knex.migrate.latest();
  console.log('[db] Migrations complete');
}
```
  </action>
  <verify>
    <automated>cat api/src/db.ts | grep "runMigrations" && cat api/src/db.ts | grep "NUMERIC\|decimal(.*20"</automated>
  </verify>
  <done>api/src/db.ts exists with runMigrations export and NUMERIC(20,2) convention comment</done>
</task>

</tasks>

<verification>
- `api/package.json` has express, knex, pg, cors as dependencies
- `api/tsconfig.json` includes migrations/ in compile scope
- `api/Dockerfile` runs `npm run dev` via ts-node-dev
- `api/src/db.ts` exports both `knex` and `runMigrations`
- `api/src/db.ts` reads DATABASE_URL from environment
- NUMERIC(20,2) convention is documented in db.ts
</verification>

<success_criteria>
- All 4 files exist with correct content
- No TypeScript errors in db.ts when checked standalone
- db.ts connects to PostgreSQL via process.env.DATABASE_URL
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` with what was built, key decisions, and any deviations from this plan.
</output>
